# 🔒 Настройка автоматического обновления сертификатов Let's Encrypt

Это руководство поможет настроить автоматическое обновление SSL сертификатов Let's Encrypt для проекта volleybot.

## 📋 Содержание

- [Предварительные требования](#предварительные-требования)
- [Установка Certbot](#установка-certbot)
- [Получение первого сертификата](#получение-первого-сертификата)
- [Настройка автоматического обновления](#настройка-автоматического-обновления)
- [Интеграция с проектом](#интеграция-с-проектом)
- [Проверка и тестирование](#проверка-и-тестирование)
- [Решение проблем](#решение-проблем)

## 🔧 Предварительные требования

1. **Домен** с настроенной DNS записью, указывающей на ваш сервер
2. **Доступ к серверу** с правами sudo
3. **Открытые порты** 80 и 443 для HTTP-01 challenge (или 443 для TLS-ALPN-01)
4. **Docker** установлен и запущен

## 📦 Установка Certbot

### Ubuntu/Debian

```bash
sudo apt update
sudo apt install certbot
```

### CentOS/RHEL

```bash
sudo yum install certbot
# или для новых версий
sudo dnf install certbot
```

### Проверка установки

```bash
certbot --version
```

## 🎫 Получение первого сертификата

### Вариант 1: Standalone (рекомендуется для первого получения)

Этот метод временно запускает веб-сервер на порту 80 для проверки домена.

```bash
# Остановите веб-сервер (nginx/apache), если он запущен
sudo systemctl stop nginx  # или apache2

# Получите сертификат
sudo certbot certonly --standalone -d yourdomain.com

# Запустите веб-сервер обратно
sudo systemctl start nginx  # или apache2
```

### Вариант 2: Webroot (если используется Nginx/Apache)

```bash
sudo certbot certonly --webroot \
  -w /var/www/html \
  -d yourdomain.com
```

### Вариант 3: DNS Challenge (для доменов без публичного веб-сервера)

```bash
sudo certbot certonly --manual --preferred-challenges dns -d yourdomain.com
```

После получения сертификаты будут находиться в:
```
/etc/letsencrypt/live/yourdomain.com/
├── fullchain.pem  # Полная цепочка сертификатов
├── privkey.pem    # Приватный ключ
├── cert.pem       # Сертификат
└── chain.pem      # Промежуточные сертификаты
```

## ⚙️ Настройка автоматического обновления

### Метод 1: Systemd Timer (рекомендуется)

Certbot автоматически создаёт systemd timer для обновления сертификатов. Проверьте его статус:

```bash
# Проверка статуса таймера
sudo systemctl status certbot.timer

# Включение автозапуска
sudo systemctl enable certbot.timer

# Запуск таймера
sudo systemctl start certbot.timer

# Просмотр логов
sudo journalctl -u certbot.timer
```

Таймер автоматически запускает обновление дважды в день и обновляет сертификаты, если до истечения осталось менее 30 дней.

### Метод 2: Cron (альтернатива)

Если systemd недоступен, используйте cron:

```bash
# Откройте crontab
sudo crontab -e

# Добавьте строку для автоматического обновления (каждый день в 3:00)
0 3 * * * certbot renew --quiet --deploy-hook "/path/to/volleybot/update_certs.sh yourdomain.com /path/to/volleybot"
```

## 🔗 Интеграция с проектом

### Шаг 1: Создание скрипта обновления

Скрипт `update_certs.sh` уже создан в корне проекта. Сделайте его исполняемым:

```bash
chmod +x update_certs.sh
```

### Шаг 2: Настройка deploy hook для Certbot

Создайте скрипт, который будет автоматически вызываться после обновления сертификатов:

```bash
# Создайте директорию для hooks
sudo mkdir -p /etc/letsencrypt/renewal-hooks/deploy

# Создайте скрипт deploy hook
sudo nano /etc/letsencrypt/renewal-hooks/deploy/volleybot-update.sh
```

Содержимое скрипта:

```bash
#!/bin/bash
# Deploy hook для обновления сертификатов volleybot

DOMAIN="yourdomain.com"
PROJECT_PATH="/path/to/volleybot"
UPDATE_SCRIPT="${PROJECT_PATH}/update_certs.sh"

# Выполняем скрипт обновления
if [ -f "$UPDATE_SCRIPT" ]; then
    "$UPDATE_SCRIPT" "$DOMAIN" "$PROJECT_PATH"
else
    echo "Ошибка: скрипт обновления не найден: $UPDATE_SCRIPT"
    exit 1
fi
```

Сделайте скрипт исполняемым:

```bash
sudo chmod +x /etc/letsencrypt/renewal-hooks/deploy/volleybot-update.sh
```

**Важно:** Замените `yourdomain.com` и `/path/to/volleybot` на ваши реальные значения!

### Шаг 3: Настройка прав доступа

Убедитесь, что пользователь, от имени которого запускается скрипт, имеет права на:
- Чтение сертификатов из `/etc/letsencrypt/live/`
- Запись в директорию `./certs/` проекта
- Перезапуск Docker контейнера

Если используется Docker без sudo, добавьте пользователя в группу docker:

```bash
sudo usermod -aG docker $USER
# Выйдите и войдите снова для применения изменений
```

### Шаг 4: Тестирование обновления вручную

Проверьте работу скрипта:

```bash
# Тестовое обновление (не обновляет реальные сертификаты)
sudo certbot renew --dry-run

# Или запустите скрипт обновления вручную
./update_certs.sh yourdomain.com
```

## ✅ Проверка и тестирование

### Проверка статуса сертификатов

```bash
# Проверка срока действия сертификатов
sudo certbot certificates

# Проверка статуса systemd timer
sudo systemctl status certbot.timer

# Просмотр последних логов обновления
sudo journalctl -u certbot.service -n 50
```

### Проверка работы контейнера

```bash
# Проверка статуса контейнера
docker ps | grep volleybot

# Просмотр логов
docker logs volleybot

# Проверка наличия сертификатов в контейнере
docker exec volleybot ls -la /app/certs/
```

### Тестирование webhook

```bash
# Проверка статуса webhook через Telegram API
curl https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo

# Проверка доступности webhook
curl -k https://yourdomain.com:8443/volley_webhook
```

## 🔍 Решение проблем

### Проблема: Certbot не может получить сертификат

**Решение:**
- Убедитесь, что DNS запись указывает на ваш сервер
- Проверьте, что порты 80 и 443 открыты
- Остановите веб-сервер перед получением сертификата (для standalone метода)

### Проблема: Скрипт не может скопировать сертификаты

**Решение:**
```bash
# Проверьте права доступа
ls -la /etc/letsencrypt/live/yourdomain.com/

# Дайте права на чтение (если необходимо)
sudo chmod 755 /etc/letsencrypt/live/
sudo chmod 755 /etc/letsencrypt/live/yourdomain.com/
```

### Проблема: Контейнер не перезапускается

**Решение:**
```bash
# Проверьте, что контейнер существует
docker ps -a | grep volleybot

# Проверьте логи
docker logs volleybot

# Перезапустите вручную
docker restart volleybot
```

### Проблема: Systemd timer не работает

**Решение:**
```bash
# Проверьте статус
sudo systemctl status certbot.timer

# Перезапустите timer
sudo systemctl restart certbot.timer

# Проверьте конфигурацию
sudo systemctl cat certbot.timer
```

### Проблема: Сертификаты не обновляются автоматически

**Решение:**
1. Проверьте, что timer активен: `sudo systemctl is-active certbot.timer`
2. Проверьте логи: `sudo journalctl -u certbot.service`
3. Запустите обновление вручную: `sudo certbot renew --dry-run`

## 📝 Дополнительные настройки

### Настройка email уведомлений

Certbot может отправлять уведомления на email при обновлении сертификатов:

```bash
sudo certbot register --email your-email@example.com
```

### Настройка автоматического обновления для нескольких доменов

Если у вас несколько доменов, создайте отдельный deploy hook для каждого или модифицируйте скрипт для обработки нескольких доменов.

### Использование с Nginx

Если вы используете Nginx как reverse proxy, настройте его для автоматической перезагрузки после обновления сертификатов:

```bash
# В deploy hook добавьте:
sudo systemctl reload nginx
```

## 🔐 Безопасность

1. **Приватные ключи:** Убедитесь, что файлы `privkey.pem` имеют права доступа 600
2. **Резервное копирование:** Регулярно создавайте резервные копии сертификатов
3. **Мониторинг:** Настройте мониторинг срока действия сертификатов

## 📚 Полезные ссылки

- [Официальная документация Certbot](https://certbot.eff.org/docs/)
- [Let's Encrypt документация](https://letsencrypt.org/docs/)
- [Docker документация](https://docs.docker.com/)

---

**Примечание:** Замените все вхождения `yourdomain.com` и `/path/to/volleybot` на ваши реальные значения перед использованием!




