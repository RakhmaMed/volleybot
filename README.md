# 🏐 Telegram Бот для Организации Опросов

Telegram-бот для автоматической организации опросов с возможностью планирования по расписанию. Идеально подходит для организации спортивных мероприятий, встреч и других регулярных событий.

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Настройка](#-настройка)
- [Запуск](#-запуск)
  - [Локальный запуск](#локальный-запуск)
  - [Запуск через Docker](#запуск-через-docker)
- [Настройка Webhook](#-настройка-webhook)
- [Деплой на сервер](#-деплой-на-сервер)
- [Тестирование](#-тестирование)
- [Команды бота](#-команды-бота)
- [Решение проблем](#-решение-проблем)
- [Структура проекта](#-структура-проекта)

## 🎯 Возможности

- ✅ **Автоматические опросы** — отправка опросов по расписанию (день недели + время)
- ✅ **Автозакрытие опросов** — автоматическое закрытие и подведение итогов
- ✅ **Управление участниками** — отслеживание списка участников
- ✅ **Команды администратора** — управление ботом через Telegram
- ✅ **Два режима работы** — Polling и Webhook
- ✅ **Docker поддержка** — простой деплой в контейнере
- ✅ **Полное покрытие тестами** — автоматические тесты с pytest

## 💻 Требования

- Python 3.12+
- aiogram 3.x
- Docker (опционально, для деплоя)
- Домен с SSL сертификатом (для режима webhook)

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd volleybot
```

### 2. Создайте `config.json`

```json
{
    "telegram_token": "YOUR_BOT_TOKEN",
    "chat_id": "YOUR_CHAT_ID",
    "admin_username": "YOUR_USERNAME",
    "polls": [
        {
            "name": "опрос для среды",
            "message": "Волейбол завтра (среда) в 18:00",
            "open_day": "tue",
            "open_hour_utc": 15,
            "open_minute_utc": 0,
            "close_day": "wed",
            "close_hour_utc": 15,
            "close_minute_utc": 0
        }
    ]
}
```

### 3. Установите зависимости

**Используя универсальный скрипт (рекомендуется):**

```bash
# Linux/macOS:
chmod +x manage.sh
./manage.sh setup

# Windows PowerShell:
.\manage.ps1 setup
```

**Или вручную с использованием uv:**

```bash
# Установка uv
# Windows PowerShell:
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

# Linux/macOS:
curl -LsSf https://astral.sh/uv/install.sh | sh

# Создание venv и установка зависимостей
uv venv
uv pip install -r requirements.txt

# Активация venv
# Windows PowerShell:
.\.venv\Scripts\Activate.ps1
# Linux/macOS:
source .venv/bin/activate
```

### 4. Запустите бота

```bash
python -m src.bot
```

## ⚙️ Настройка

### Конфигурационный файл `config.json`

**Минимальная конфигурация (режим polling):**

```json
{
    "telegram_token": "YOUR_BOT_TOKEN",
    "chat_id": "YOUR_CHAT_ID",
    "admin_username": "YOUR_USERNAME",
    "polls": []
}
```

**Полная конфигурация (с webhook):**

```json
{
    "telegram_token": "YOUR_BOT_TOKEN",
    "chat_id": "YOUR_CHAT_ID",
    "admin_username": "YOUR_USERNAME",
    "webhook_host": "https://yourdomain.com",
    "webhook_path": "/webhook",
    "webhook_port": 8443,
    "ssl_cert_path": "/app/certs/fullchain.pem",
    "ssl_key_path": "/app/certs/privkey.pem",
    "polls": [
        {
            "name": "название опроса",
            "message": "текст сообщения с опросом",
            "open_day": "mon",
            "open_hour_utc": 10,
            "open_minute_utc": 0,
            "close_day": "mon",
            "close_hour_utc": 18,
            "close_minute_utc": 0
        }
    ]
}
```

**Параметры:**

- `telegram_token` — токен бота от @BotFather
- `chat_id` — ID чата/группы (для групп начинается с минуса)
- `admin_username` — username администратора (без @)
- `webhook_host` — URL вашего домена (если используете webhook)
- `webhook_path` — путь для webhook (например, `/webhook`)
- `webhook_port` — порт для webhook (обычно 8443)
- `ssl_cert_path` — путь к SSL сертификату
- `ssl_key_path` — путь к приватному ключу SSL
- `polls` — массив опросов с расписанием

**Дни недели:** `mon`, `tue`, `wed`, `thu`, `fri`, `sat`, `sun`

**Время:** указывается в UTC (не забудьте скорректировать для вашего часового пояса)

## 🏃 Запуск

### Локальный запуск

**Режим polling (по умолчанию):**

```bash
python -m src.bot
```

Бот будет опрашивать Telegram API для получения обновлений.

### Запуск через Docker

#### Вариант 1: Прямой запуск

**Сборка образа:**

```bash
docker build -t volleybot:latest .
```

**Запуск (polling режим):**

```bash
docker run -d \
  --name volleybot \
  --restart unless-stopped \
  -v $(pwd)/config.json:/app/config.json:ro \
  volleybot:latest
```

**Запуск (webhook режим):**

```bash
docker run -d \
  --name volleybot \
  --restart unless-stopped \
  -p 8443:8443 \
  -v $(pwd)/certs:/app/certs:ro \
  -v $(pwd)/config.json:/app/config.json:ro \
  volleybot:latest
```

**Просмотр логов:**

```bash
docker logs -f volleybot
```

**Управление контейнером:**

```bash
# Остановить
docker stop volleybot

# Запустить
docker start volleybot

# Перезапустить
docker restart volleybot

# Удалить
docker rm volleybot
```

#### Вариант 2: Docker Compose (рекомендуется)

Файл `docker-compose.yml` уже создан в корне проекта. Просто запустите:

**Запуск:**

```bash
docker-compose up -d
```

**Или используйте скрипт управления:**

```bash
./manage.sh deploy
```

**Просмотр логов:**

```bash
docker-compose logs -f
```

**Обновление:**

```bash
docker-compose up -d --build
```

## 🔗 Настройка Webhook

Webhook обеспечивает более эффективную работу бота по сравнению с polling.

### Вариант 1: Прямое подключение (без Nginx)

#### Шаг 1: Подготовка сертификатов

```bash
# На сервере создайте директорию для сертификатов
mkdir -p certs

# Скопируйте Let's Encrypt сертификаты
sudo cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem ./certs/
sudo cp /etc/letsencrypt/live/yourdomain.com/privkey.pem ./certs/

# Установите права доступа
sudo chmod 644 ./certs/fullchain.pem
sudo chmod 600 ./certs/privkey.pem
sudo chown $USER:$USER ./certs/*
```

#### Шаг 2: Настройка файрвола

```bash
# Для UFW (Ubuntu)
sudo ufw allow 8443/tcp

# Для firewalld (CentOS/RHEL)
sudo firewall-cmd --permanent --add-port=8443/tcp
sudo firewall-cmd --reload
```

#### Шаг 3: Обновление config.json

```json
{
    "webhook_host": "https://yourdomain.com",
    "webhook_path": "/webhook",
    "webhook_port": 8443,
    "ssl_cert_path": "/app/certs/fullchain.pem",
    "ssl_key_path": "/app/certs/privkey.pem"
}
```

#### Шаг 4: Запуск

```bash
docker run -d \
  --name volleybot \
  --restart unless-stopped \
  -p 8443:8443 \
  -v $(pwd)/certs:/app/certs:ro \
  -v $(pwd)/config.json:/app/config.json:ro \
  volleybot:latest
```

#### Шаг 5: Проверка

```bash
# Проверка статуса webhook через Telegram API
curl https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo

# Проверка доступности webhook
curl -k https://yourdomain.com:8443/webhook
```

### Вариант 2: Через Nginx (рекомендуется для продакшена)

#### Шаг 1: Установка Nginx

```bash
sudo apt update
sudo apt install nginx
```

#### Шаг 2: Настройка Nginx

Создайте `/etc/nginx/sites-available/volleybot`:

```nginx
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location /webhook {
        proxy_pass https://localhost:8443;
        proxy_ssl_verify off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/volleybot /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### Шаг 3: Обновление config.json

```json
{
    "webhook_host": "https://yourdomain.com",
    "webhook_path": "/webhook",
    "webhook_port": 8443
}
```

При использовании Nginx порт в URL не указывается.

### Автообновление сертификатов

Для автоматического обновления сертификатов Let's Encrypt используйте скрипт `update_certs.sh`, который уже включён в проект.

#### Быстрая настройка

1. **Сделайте скрипт исполняемым:**
```bash
chmod +x update_certs.sh
```

2. **Запустите скрипт вручную для первого обновления:**
```bash
./update_certs.sh yourdomain.com
```

3. **Настройте автоматическое обновление через Certbot:**

Certbot автоматически обновляет сертификаты через systemd timer. Настройте deploy hook для автоматического обновления в проекте:

```bash
# Создайте deploy hook
sudo mkdir -p /etc/letsencrypt/renewal-hooks/deploy
sudo nano /etc/letsencrypt/renewal-hooks/deploy/volleybot-update.sh
```

Содержимое скрипта (замените `yourdomain.com` и `/path/to/volleybot`):
```bash
#!/bin/bash
/path/to/volleybot/update_certs.sh yourdomain.com /path/to/volleybot
```

```bash
sudo chmod +x /etc/letsencrypt/renewal-hooks/deploy/volleybot-update.sh
```

4. **Проверьте статус автоматического обновления:**
```bash
sudo systemctl status certbot.timer
sudo systemctl enable certbot.timer
```

#### Подробная инструкция

Для полной настройки автоматического обновления сертификатов см. подробное руководство: **[CERTBOT_SETUP.md](CERTBOT_SETUP.md)**

#### Использование Docker Compose

Если вы используете Docker Compose, обновление сертификатов будет автоматически подхватываться при перезапуске контейнера:

```bash
# После обновления сертификатов
./update_certs.sh yourdomain.com
# или
docker-compose restart bot
```

## 🌐 Деплой на сервер

### Предварительные требования

1. Docker установлен на сервере
2. Файлы проекта загружены на сервер
3. `config.json` настроен с правильными данными
4. (Для webhook) Домен с SSL сертификатом

### Установка Docker

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Проверка
docker --version
```

### Деплой

```bash
# 1. Перейдите в директорию проекта
cd /path/to/volleybot

# 2. Соберите образ
docker build -t volleybot:latest .

# 3. Запустите контейнер
docker run -d \
  --name volleybot \
  --restart unless-stopped \
  -p 8443:8443 \
  -v $(pwd)/certs:/app/certs:ro \
  -v $(pwd)/config.json:/app/config.json:ro \
  volleybot:latest

# 4. Проверьте логи
docker logs -f volleybot
```

### Обновление бота

```bash
# Остановите и удалите старый контейнер
docker stop volleybot
docker rm volleybot

# Обновите код (например, через git pull)
git pull

# Пересоберите образ
docker build -t volleybot:latest .

# Запустите новый контейнер
docker run -d \
  --name volleybot \
  --restart unless-stopped \
  -p 8443:8443 \
  -v $(pwd)/certs:/app/certs:ro \
  -v $(pwd)/config.json:/app/config.json:ro \
  volleybot:latest
```

Или используйте универсальный скрипт управления:

```bash
# Linux/macOS:
./manage.sh deploy

# Windows PowerShell:
.\manage.ps1 deploy
```

## 🧪 Тестирование

Проект имеет полное покрытие автоматическими тестами.

### Структура тестов

- `tests/test_utils.py` — тесты утилит
- `tests/test_config.py` — тесты загрузки конфигурации
- `tests/test_poll.py` — тесты логики опросов
- `tests/test_handlers.py` — тесты обработчиков команд
- `tests/test_scheduler.py` — тесты планировщика
- `tests/conftest.py` — общие фикстуры

### Быстрый старт с тестами

**Используя универсальный скрипт управления:**

```bash
# Linux/macOS:
./manage.sh setup         # Настройка окружения
./manage.sh test          # Запуск тестов
./manage.sh test -c       # С покрытием кода
./manage.sh test -v       # С подробным выводом

# Windows PowerShell:
.\manage.ps1 setup        # Настройка окружения
.\manage.ps1 test         # Запуск тестов
.\manage.ps1 test -Coverage  # С покрытием кода
.\manage.ps1 test -Verbose   # С подробным выводом
```

### Ручной запуск

**С использованием uv:**

```bash
# Установка зависимостей
uv venv
uv pip install -r requirements.txt

# Активация venv
source .venv/bin/activate  # Linux/macOS
.\.venv\Scripts\Activate.ps1  # Windows PowerShell

# Запуск всех тестов
pytest

# С покрытием кода
pytest --cov=. --cov-report=html

# Конкретный файл
pytest tests/test_utils.py

# Конкретный тест
pytest tests/test_utils.py::TestIsAdmin::test_is_admin_with_correct_username

# Подробный вывод
pytest -v

# С выводом print-ов
pytest -s
```

**Без активации venv (через uv run):**

```bash
uv run pytest
uv run pytest --cov=. --cov-report=html
```

### Просмотр отчёта о покрытии

После запуска с `--cov-report=html` откройте `htmlcov/index.html` в браузере.

## 🤖 Команды бота

Все команды доступны только администратору (указанному в `admin_username`).

- `/start` — Включить бота
- `/stop` — Выключить бота
- `/status` — Показать статус бота
- `/send_poll` — Отправить опрос вручную
- `/close_poll` — Закрыть активный опрос
- `/list` — Показать список участников
- `/help` — Показать справку

## 🔧 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
docker logs volleybot

# Проверьте конфигурацию
docker exec volleybot cat /app/config.json

# Проверьте наличие токена
grep telegram_token config.json
```

### Ошибки подключения

- Проверьте правильность токена в `config.json`
- Убедитесь, что `chat_id` указан правильно (с минусом для групп)
- Проверьте доступ к интернету

### Webhook не работает

```bash
# Проверьте статус webhook
curl https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo

# Проверьте сертификаты в контейнере
docker exec volleybot ls -la /app/certs/

# Проверьте доступность домена
curl -I https://yourdomain.com/webhook

# Проверьте логи Nginx (если используется)
sudo tail -f /var/log/nginx/error.log
```

### Проблемы со временем

- Проверьте часовой пояс сервера: `date`
- Время в `config.json` указывается в UTC
- Скорректируйте `hour_utc` и `minute_utc` для вашего часового пояса

### Переключение на polling

Если webhook не работает, временно переключитесь на polling:

1. Удалите или закомментируйте `webhook_host` в `config.json`
2. Перезапустите контейнер: `docker restart volleybot`

### Конфликт имени контейнера

```bash
docker stop volleybot
docker rm volleybot
# Затем запустите контейнер снова
```

## 🛠️ Универсальный скрипт управления

Для удобства работы с проектом созданы универсальные скрипты управления:

- `manage.sh` — для Linux/macOS
- `manage.ps1` — для Windows PowerShell

### Доступные команды:

```bash
# Настройка окружения
./manage.sh setup          # Linux/macOS
.\manage.ps1 setup         # Windows

# Тестирование
./manage.sh test                    # Запустить все тесты
./manage.sh test -c                 # С покрытием кода
./manage.sh test -v                 # Подробный вывод
./manage.sh test -f tests/test_utils.py  # Конкретный файл

# Docker операции
./manage.sh deploy         # Собрать и запустить контейнер
./manage.sh logs           # Показать логи
./manage.sh start          # Запустить контейнер
./manage.sh stop           # Остановить контейнер
./manage.sh restart        # Перезапустить контейнер
./manage.sh status         # Показать статус
./manage.sh clean          # Удалить контейнер и образ

# Справка
./manage.sh help           # Показать все команды
```

## 📁 Структура проекта

```
volleybot/
├── src/                # Исходный код приложения
│   ├── __init__.py
│   ├── bot.py         # Точка входа, инициализация бота
│   ├── config.py      # Загрузка конфигурации
│   ├── handlers.py    # Обработчики команд
│   ├── poll.py        # Логика опросов
│   ├── scheduler.py   # Планировщик задач
│   └── utils.py       # Вспомогательные функции
├── tests/             # Автотесты
│   ├── conftest.py
│   ├── test_config.py
│   ├── test_handlers.py
│   ├── test_poll.py
│   ├── test_scheduler.py
│   └── test_utils.py
├── certs/             # SSL сертификаты (создаётся автоматически, не в git!)
│   ├── fullchain.pem
│   └── privkey.pem
├── config.json        # Конфигурация (не в git!)
├── requirements.txt   # Зависимости Python
├── Dockerfile         # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
├── update_certs.sh    # Скрипт автоматического обновления сертификатов
├── renewal-hooks-example.sh  # Пример deploy hook для Certbot
├── CERTBOT_SETUP.md   # Подробная инструкция по настройке Let's Encrypt
├── pytest.ini         # Настройки pytest
├── .coveragerc        # Настройки coverage
├── manage.sh          # Универсальный скрипт управления (Linux/macOS)
└── manage.ps1         # Универсальный скрипт управления (Windows)
```

## 📝 Важные замечания

1. **Безопасность:** Никогда не коммитьте `config.json` с реальными токенами в Git
2. **Логи:** Регулярно проверяйте логи на наличие ошибок
3. **Время:** Убедитесь, что на сервере правильно настроено время (важно для планировщика)
4. **Сеть:** Убедитесь, что сервер имеет доступ к интернету для работы с Telegram API
5. **Сертификаты:** При обновлении Let's Encrypt сертификатов не забудьте обновить их в контейнере

## 📄 Лицензия

Этот проект распространяется свободно. Используйте на свое усмотрение.

## 🤝 Вклад

Приветствуются pull requests и issue reports!

---

**Разработано с ❤️ для организации спортивных мероприятий**

